<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to "Finance Tracker", root_path, class:"navbar-brand"%>
    <% if current_user %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to "My Portfolio", my_portfolio_path, class:"nav-link #{'active' if request.path == my_portfolio_path}"%>
          </li>
          <li class="nav-item">
            <%= link_to "My Friends", my_friends_path, class:"nav-link #{'active' if request.path == my_friends_path}"%>
          </li>
        </ul>
        <ul class='navbar-nav ml-auto'>
          <li class="nav-nav dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= fa_icon 'user' %> <%= current_user.full_name%>
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to edit_user_registration_path, class: 'dropdown-item' do %>
                <%= fa_icon 'edit' %> Edit Profile
              <% end %>
            </ul>
          </li>
          <li class='nav-item'>
            <%= link_to "Logout", destroy_user_session_path, class:"btn btn-danger logout-button", method: :delete %>
          </li>
        </ul>
      </div>
    <% end %>
  </div>
</nav>